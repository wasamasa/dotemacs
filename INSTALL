1. Clone the repository
2. Install cask
3. Run cask in ~/.emacs.d
4. Install the jedi python dependency (python-jedi from the regular repositories)
5. Install boris (boris from the AUR)
6. Create the directory ~/.emacs.d/etc
